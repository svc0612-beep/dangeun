# 사전 예방 시스템 — 설계 문서

> 당근 클론에 **사기 사전 예방**을 붙이는 작업의 설계도.
> 지금까지 대화로 정한 결정과 근거를 한곳에 모은 것. 빌드는 이 문서를 기준으로 진행한다.

---

## 0. 목표 & 원칙

**목표**: 지금은 사기가 벌어진 뒤 신고를 조사하는 **사후 탐지**다. 여기에 사기를 벌어지기 전에 막는 **사전 예방**을 더한다.

**3대 원칙** (기존 프로젝트 철학 그대로 이어감):
1. **행동을 지목, 사람을 낙인찍지 않는다** — "이 사람 사기꾼"이 아니라 "이 거래 방식이 위험".
2. **판정은 규칙, 최종 결정은 사람** — 무거운 제재(정지)는 항상 관리자 확인.
3. **처벌은 족쇄가 아니라 교화** — 시간이 지나면 해방, 노력하면 회복.

---

## 1. 전체 파이프라인 (6단계)

```
① 탐지        채팅 메시지에서 위험 신호 감지 (badwords)
     ↓
② 증거 기록    무엇을·언제·어느 방에서 어겼는지 저장 (violations 테이블)
     ↓
③ 누진 판정    최근 N시간 내 위반 수 세기 → 단계별 조치
     ↓
④ 관리자       문턱 넘으면 알림 + 제재 대기 목록 (증거 첨부)
     ↓
⑤ 회복        시간 지나면 카운트 해방 / 매너온도는 리뷰로만 회복
     ↓
⑥ 보관        위반 이력은 15일 뒤 삭제 (프라이버시)
```

핵심: ①~③·⑤·⑥은 **규칙이 자동**, ④의 무거운 제재만 **사람이 결정**.

---

## 2. 단계별 상세 + (우려 → 해소)

### ① 탐지
- `badwords.py`가 이미 선입금·택배·직거래거부·앱밖연락 패턴을 감지함. → **거의 완성. 패턴만 보강.**
- 메시지는 **막지 않고** 경고만 띄운다 (이미 그렇게 구현됨 — "정상적인 숫자일 수도 있으니").

### ② 증거 기록
- **(우려) "관리자가 뭘 잘못했는지 모른다"** → **(해소)** 위반을 숫자로만 세지 않고 **증거(어긴 규칙 · 실제 문장 · 시각 · 채팅방)**를 함께 저장. 관리자가 근거를 보고 판단.

### ③ 누진 판정
- 최근 `STRIKE_WINDOW_HOURS`(예: 48h) 안의 위반 수로 단계 결정:
  - 1회 → 채팅 경고만 (누구나 실수)
  - 2회 → 매너온도 하락 + 공식 경고 알림
  - 3회 → 해당 매물 숨김 검토
  - 4회+ → 관리자에게 상정
- **(우려) "규칙이 약하면 오탐으로 애먼 사람 처벌"** → **(해소)** 오탐 잦은 약한 신호는 **카운트에서 제외**. 확실한 위반(사기 신호=scam)만 누진에 셈. 욕설 등은 경고만.

### ④ 관리자
- 문턱 넘으면 `admin_notices`에 알림 + 대시보드 "제재 대기" 목록.
- 각 항목에 **증거(위반 문장)** 표시, 필요하면 **[AI 조사]** 버튼으로 조사 에이전트가 채팅 읽고 요약.
- 버튼: **[일시 정지] [무혐의]**
- **(우려) "낙인·무고"** → **(해소)** 최종 정지는 관리자 클릭. 오탐이면 **[무혐의]로 카운트 리셋**해 되돌릴 수 있음.
- **(새로 제시) 경고는 판매자·구매자 양쪽 모두에게** — 사기는 양쪽 다 칠 수 있으니, 행동을 지목해 공정하게.

### ⑤ 회복
- **활성 카운트**: `STRIKE_WINDOW_HOURS` 지나면 자동으로 빠짐(해방). **로그아웃과 무관, 시계 기준.**
- **매너온도**: 자동 회복 없음. 정당한 거래 + 좋은 리뷰로만 서서히 회복.
- **(우려) "영원한 족쇄면 안 된다"** → **(해소)** 제재는 시간이 풀어주고, 평판은 착한 거래가 회복시킴.

### ⑥ 보관
- 위반 이력은 `HISTORY_KEEP_DAYS`(예: 15일) 뒤 삭제.
- **(우려) "상습범이 리셋을 악용"** → **(해소)** 이력 보관 기간(15일)을 활성 윈도우(48h)보다 훨씬 길게 둬서, 관리자·AI가 "이틀마다 조금씩" 악용 패턴을 여전히 본다.
- 보관을 짧게 하는 진짜 이유는 용량이 아니라 **프라이버시·깔끔함**.

---

## 3. 설정값 (config.py — 조정 가능)

| 상수 | 기본값 | 뜻 |
|---|---|---|
| `PREVENTION_ON` | True | 기능 on/off |
| `STRIKE_WINDOW_HOURS` | 48 | 활성 카운트 유지 시간 (지나면 해방) |
| `HISTORY_KEEP_DAYS` | 15 | 위반 이력 보관 기간 |
| `MANNER_PENALTY` | 0.5 | 위반 1회당 매너온도 하락(도) |
| `STRIKE_STEPS` | {2:매너, 3:숨김, 4:상정} | 누적 횟수별 조치 |
| `COUNT_KINDS` | ("scam",) | 카운트에 셀 위반 종류 (욕설은 제외) |

---

## 4. DB 계획

**재사용 (이미 있음):**
- `admin_notices` — 관리자 상정 알림
- `admin_actions` — 정지·무혐의 조치 기록
- `User.manner_temp` — 매너온도 하락
- `User.is_blocked` / `blocked_reason` — 정지

**새로 추가:**
- `violations` 테이블 — 채팅 위반 증거·누진용
  - `user_id`, `rule`(어긴 규칙), `snippet`(실제 문장), `room_id`, `kind`(scam/curse…), `cleared`(무혐의 처리 여부), `created_at`(인덱스)
  - 활성 카운트 = "최근 48h 내 `cleared=False`인 내 위반 수" (쿼리로 계산, 별도 카운터 필드 불필요)

---

## 5. 파일별 변경 & 빌드 순서

로직은 채팅 코드에 욱여넣지 않고 **`prevention.py` 새 모듈**로 모은다 (파일 정리).

| 단계 | 파일 | 내용 | 프론트 |
|---|---|---|---|
| **1** | `config.py` | 설정 상수 추가 | X |
| **2** | `database.py` | `violations` 테이블 추가 | X |
| **3** | `prevention.py` (신규) | 탐지결과→기록→카운트→단계별 조치 로직 | X |
| **4** | `chats.py` | 메시지 전송 시 `prevention` 호출 | X |
| **5** | `dashboard.py`/`admin.py` + 프론트 admin | 제재 대기 목록 + [무혐의] 버튼 | O(관리자만) |
| **6** | `scripts/` | 15일 지난 이력 정리 배치 | X |
| 7(선택) | 프론트 `ChatRoom.jsx` | 경고를 눈에 띄는 배너로 승격 | O |

**대부분 백엔드.** 사용자 채팅 화면은 경고가 이미 시스템 메시지로 떠서 거의 안 건드림.

---

## 6. 하지 않는 것 (지금은)

- **진위 판별(진품/가품)** — CLIP·pHash로는 불가. **향후 장기 도전 목표**로만 둠. (당장은 사전 예방에 집중)

---

*이 문서는 빌드가 진행되며 갱신한다. 다음 작업: Step 1 — config.py 설정 상수 추가.*
